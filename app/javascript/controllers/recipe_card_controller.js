import { Controller } from "@hotwired/stimulus"

// Connects to data-controller="recipe-card"
export default class extends Controller {
  // let c = 0;
  // static targets = ["cardElement"]
  connect() {
    // console.log("Hello from recipe_card_controller.js")
  }
  // open_close() {
  //   this.cardElementTarget.className =
  //     c % 2 == 0 ? "cont_modal cont_modal_active" : "cont_modal";
  //   c++;
  // }
}
